spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        service: account
      config:
        file-extension: yaml
        group: DEFAULT_GROUP  # 缺省即可
        prefix: ${spring.application.name} # 缺省即可
        server-addr: 127.0.0.1:8848
